services:
  db:
    image: postgres:15
    container_name: rgr_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: rgr_db
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: